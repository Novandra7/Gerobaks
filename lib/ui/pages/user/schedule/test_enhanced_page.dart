import 'package:bank_sha/ui/pages/user/schedule/enhanced_add_schedule_page.dart';
import 'package:flutter/material.dart';

class TestEnhancedSchedulePage extends StatelessWidget {
  const TestEnhancedSchedulePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: AddSchedulePage(),
    );
  }
}
